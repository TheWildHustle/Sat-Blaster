import { Adapter, Callback, NodeValue, Unsubscribe } from '../types';
/**
 * An adapter that stores data in the browser's local storage and memory.
 */
export declare class LocalStorageMemoryAdapter implements Adapter {
    private storage;
    private isLoaded;
    private loadingPromise;
    private resolveLoading;
    constructor();
    private loadFromLocalStorage;
    private listFromStorage;
    get(path: string, callback: Callback): Unsubscribe;
    set(path: string, value: NodeValue): Promise<void>;
    list(path: string, callback: Callback): Unsubscribe;
}
//# sourceMappingURL=LocalStorageMemoryAdapter.d.ts.map